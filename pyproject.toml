[project]
name = "git-good"
version = "0.6.0"
description = "AI-powered commit message generation via git hooks"
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
git-good = "git_good.main:main"
